package unsw.dungeon;

/**
 * Interface representing the an Entity that can be manipulated
 * 
 * @author Sean Smith
 * @author Austin Landry
 */
public interface Interactable {

    public Boolean interact(Entity entity);
}